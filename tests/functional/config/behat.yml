# behat.yml
default:
  paths:
    features: '%behat.paths.base%/../features'
    bootstrap:  '%behat.paths.features%/bootstrap'
  extensions:
    Behat\MinkExtension\Extension:
      base_url: http://nfqakademija.dev/
      goutte: ~
      selenium2: ~
  context:
    parameters:
      browser: firefox
